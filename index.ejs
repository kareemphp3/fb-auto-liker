<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
  <meta charset="UTF-8">
  <title>Facebook Auto Liker</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.rtl.min.css" rel="stylesheet">
</head>
<body class="container py-5">
  <h1 class="mb-4">FB Auto Liker</h1>
  <form action="/like" method="POST">
    <div class="mb-3">
      <label for="cookies" class="form-label">الكوكيز</label>
      <textarea name="cookies" id="cookies" class="form-control" rows="3" required></textarea>
    </div>
    <div class="mb-3">
      <label for="postUrl" class="form-label">رابط البوست</label>
      <input type="url" name="postUrl" id="postUrl" class="form-control" placeholder="https://facebook.com/..." required>
    </div>
    <button type="submit" class="btn btn-primary">نفّذ لايك</button>
  </form>

  <% if (message) { %>
    <div class="alert alert-info mt-3"><%= message %></div>
  <% } %>
</body>
</html>
